<?php

namespace SLLH\StyleCIFixers;

/**
 * This class was auto-generated from StyleCI/SDK tool.
 *
 * @link https://github.com/StyleCI/SDK
 */
class Fixers
{
{% for group,fixers in fixersTab %}
    /**
     * @var string[]
     */
    public static ${{ group }} = array(
{% for fixer in fixers %}
{% if fixer['key'] is defined %}
        '{{ fixer['key'] }}' => '{{ fixer['name'] }}',
{% else %}
        '{{ fixer['name'] }}',
{% endif %}
{% endfor %}
    );
{% if not loop.last %}

{% endif %}
{% endfor %}

    /**
     * @return string[][]
     */
    public static function getPresets()
    {
        return array(
{% for preset in presets %}
            '{{ preset }}' => self::${{ preset }}_fixers,
{% endfor %}
        );
    }
}
